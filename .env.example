# Optional: Etherscan API key for fetching transactions
ETHERSCAN_API_KEY=

# Optional: restrict CORS for production (comma-separated origins)
FRONTEND_ORIGIN=

# Optional: enforce HTTPS forwarding (set true on hosted)
ENFORCE_HTTPS=false

# Rate limit per minute
RATE_LIMIT_PER_MIN=60